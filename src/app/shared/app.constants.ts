/**
 * @file Application-wide identity constants shared across the UI.
 */

/** Public product name shown in headings, brand copy and document metadata. */
export const APP_NAME = 'Vibo';

/** Display name of the single workspace shown in the topbar and side menu. */
export const WORKSPACE_NAME = 'Devspace';
